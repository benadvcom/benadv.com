Fixed the responsive navigation:
- Below 900px the header uses two rows.
- Logo + language/theme controls stay on the first row.
- Full primary navigation moves to a visible second row.
- Menu is horizontally scrollable on narrow screens instead of clipping.
- Active page underline remains visible.
- Works in both light and dark themes.
